{
  "name": "quant-workstation",
  "version": "1.0.0",
  "description": "Elite 10 Quant System - Single Service Deployment",
  "private": true,
  "scripts": {
    "postinstall": "npm install --include=dev --prefix frontend && npm install --include=dev --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "build:backend": "npm run build --prefix backend",
    "build": "npm run build:frontend && npm run build:backend && npx playwright install chromium",
    "start": "node backend/dist/app.js",
    "dev": "npm run dev --prefix backend"
  }
}
